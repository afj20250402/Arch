# 配布ファイル（docs/dist/）は**追跡します**。ここに ignore を書きません。
# 配信は Git に入っている内容が対象なので、追跡しないと配布ファイルがどこにも公開されません。
# 使い捨ての鍵で署名したものが混ざる心配は、コミット前の検査で止まるようにしてあります。

# 鍵ファイルの置き間違いに対する最後の砦。この配下は「全てそのまま公開してよい」ことが
# 不変条件であり、秘密が1バイトも入ってはならない。
*.key.json
